package grpc

// Package grpc wires the internal gRPC transport (Access.Check) used by other
// microservices. The generated protobuf code lives in the pb subpackage and the
// per-RPC handler in handlers.
